version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22
    commands:
      - npm install
      - cd gremlin-eye.client

  build:
    commands:
      - echo "Building the frontend React application..."
      - npm run build
  
  artifacts:
    files:
      - "**/*"
    base-directory: gremlin-eye.client/dist

  cache:
    paths:
      - gremlin-eye.client/node_modules/**/*